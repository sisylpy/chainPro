<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.grain.dao.CkOutDepDao">

	<select id="queryObject" resultType="com.grain.entity.CkOutDepEntity">
		select * from ck_out_dep where out_dep_id = #{value}
	</select>

	<select id="queryList" resultType="com.grain.entity.CkOutDepEntity">
		select * from ck_out_dep 
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from ck_out_dep 
	</select>
    <select id="queryAll" resultType="com.grain.entity.CkOutDepEntity">
		select * from ck_out_dep
	</select>

    <insert id="save" parameterType="com.grain.entity.CkOutDepEntity">
		insert into ck_out_dep
		(
			`out_dep_id`, 
			`out_dep_name`
		)
		values
		(
			#{outDepId}, 
			#{outDepName}
		)
	</insert>
	 
	<update id="update" parameterType="com.grain.entity.CkOutDepEntity">
		update ck_out_dep 
		<set>
			<if test="outDepName != null">`out_dep_name` = #{outDepName}</if>
		</set>
		where out_dep_id = #{outDepId}
	</update>
	
	<delete id="delete">
		delete from ck_out_dep where out_dep_id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from ck_out_dep where out_dep_id in 
		<foreach item="outDepId" collection="array" open="(" separator="," close=")">
			#{outDepId}
		</foreach>
	</delete>

</mapper>